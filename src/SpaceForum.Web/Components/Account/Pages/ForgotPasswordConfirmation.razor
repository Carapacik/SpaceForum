@page "/account/forgotpasswordconfirmation"

<PageTitle>@T["Check your email"] · SpaceForum</PageTitle>

<section class="sf-auth-shell sf-card p-6 text-center medium:p-8">
    <p class="sf-eyebrow">@T["Account recovery"]</p>
    <h1 class="mt-2 font-display text-2xl font-bold">@T["Check your email"]</h1>
    <p class="mt-3 text-content-muted" role="alert">@T["Check email reset"]</p>
</section>
